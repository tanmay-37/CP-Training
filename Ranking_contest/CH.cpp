#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    int x, y, z;
    while (t--)
    {
        cin >> x >> y >> z;
        int pointsNeeded = y - x;
        if(pointsNeeded){}
    }
    return 0;
}